package com.java_mentor.service;

public interface Calculator {
    String plus(int x, int y, int type);

    String minus(int x, int y, int type);
    String multiply(int x, int y, int type);
    String division(int x, int y, int type);
}
